# Root Environment Variables Template
# This file documents shared environment variables
# Copy to .env for local development (optional - most vars go in app/service specific files)

# ====================
# WORKSPACE SETTINGS
# ====================

# NX Cloud (Optional - for distributed caching)
# Get token from: https://nx.app/
NX_CLOUD_ACCESS_TOKEN=

# ====================
# SHARED DATABASE (Development Only)
# ====================

# Shared Postgres for local development
DATABASE_URL=postgresql://postgres:password@localhost:5432/harness_hero

# Shared Redis for local development
REDIS_URL=redis://localhost:6379

# ====================
# NOTE
# ====================
# Most environment variables are service-specific:
# - Frontend vars → apps/web/.env.local (copy from apps/web/env.template)
# - Backend vars → services/backend/.env (copy from services/backend/env.template)
# - Micro-frontend vars → apps/[name]/.env.local
# - Microservice vars → services/[name]/.env

